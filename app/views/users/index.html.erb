

<h1>Users</h1>
<div class="user">
  <table width="70%">
    <thead>
      <tr>
        <th>name</th>
        <th>face</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td width="30%"><%= user.name %></td>
          <td width="30%"><%= image_tag(user.face.url) %></td>
          <% if logged_in? %>
            <% if user.id == current_user.id %>
              <td width="20%"><%= link_to '詳細', user %></td>
              <td width="20%"><%= link_to '編集', edit_user_path(user) %></td>
            <% end %>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>


<br>
